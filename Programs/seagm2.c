#include <stdio.h>
#include <stdlib.h>

double P[20][10000 + 10];

int main(){
	int T;
	int tn = 1;
	scanf("%lld", &T);

	long long N, M;
	while(tn <= T){
		scanf("%lld", &N, &M);

		tn++;
	}

	return 0;
}